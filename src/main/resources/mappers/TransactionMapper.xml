<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bankATM.mapper.TransactionMapper">

    <insert id="insert"
            parameterType="bankATM.entity.Transaction">

        INSERT INTO transactions(
        type,
        from_account_id,
        to_account_id,
        amount,
        from_currency,
        to_currency,
        exchange_rate,
        created_at
        )
        VALUES (
        #{type},
        #{fromAccountId},
        #{toAccountId},
        #{amount},
        #{fromCurrency},
        #{toCurrency},
        #{exchangeRate},
        NOW()
        )

    </insert>

    <select id="findByAccountId"
            resultType="bankATM.entity.Transaction">

        SELECT *
        FROM transactions
        WHERE from_account_id = #{accountId}
        OR to_account_id = #{accountId}
        ORDER BY created_at DESC

    </select>

</mapper>